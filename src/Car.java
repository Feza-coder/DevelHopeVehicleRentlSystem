public class Car extends Vehicle {
    public Car(String licensePlate, String color, double pricePerDay) {
        super(licensePlate, color, pricePerDay);
    }
}

class Truck extends Vehicle {
    public Truck(String licensePlate, String color, double pricePerDay) {
        super(licensePlate, color, pricePerDay);
    }
}

class Motorcycle extends Vehicle {
    public Motorcycle(String licensePlate, String color, double pricePerDay) {
        super(licensePlate, color, pricePerDay);
    }
}
